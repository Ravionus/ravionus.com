name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install test dependencies
        run: npm install
        working-directory: tests

      - name: Install Playwright browser
        run: npx playwright install --with-deps chromium
        working-directory: tests

      - name: Run tests
        run: npx playwright test
        working-directory: tests

      # If any tests are still failing after Playwright's own retries,
      # re-run only those failed tests one more time so we can confirm
      # whether it's a genuine failure or a one-off environment issue.
      - name: Re-run failed tests
        if: failure()
        run: npx playwright test --last-failed
        working-directory: tests

      - name: Upload report on failure
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: tests/playwright-report/
          retention-days: 7
